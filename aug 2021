/*
#__________________________________________________________#
#Generated by:                                NITIN KUMAR  #
#                                            ASET,BIJWASEN #
#												IIIT HYD   #
#__________________________________________________________#
*/
#include <bits/stdc++.h>

using namespace std;

#define endl '\n'
#define CLEAR cout << flush
#define EB emplace_back
#define NL putchar_unlocked(\n)
#define mod 1000000007
#define ll long long
#define ull unsigned ll
#define VI vector <int>
#define SI set<int>
#define VLL vector <ll>
#define VS vector <string>
#define MAPII map <int,int>
#define PB push_back
#define MP make_pair
#define GCD(x,y) __gcd(x, y)
#define LCM(x,y) x*y/__gcd(x, y)
#define deb(x) cout << #x << " " << x << endl;//use(C++): deb(x);
#define deb_c(var) printf("%s:%d \n", #var,var)//use(C): int myVar=100; deb_c(myVar);

//vector <int> ans; //ans.reserve(1000); 
//vector<int> arr(n, 0);//avoids use of reserve also 
template<typename... T> //use: read(x, y, z, zz);
void read(T&... args) {
	((cin >> args), ...);
}
template<typename... T> //use: write(x, y, z, zz, "This is awesome right?", 4.0);
void write(T&&... args) { //rvalue reference is new to C++ (can use write(T... args)  also
	((cout << args << " "), ...);
}
#define CPYAR(Arr,x,Arr1) copy_n(Arr, x, Arr1);//copy x elements from Arr to Arr1
#define GIVE(Arr,y,z)  iota(Arr, Arr+y, z);// Using iota() to assign values assign values to array Arr starting from z next element having +1 value upto y elements
#define ALL_POS( a , size ) all_of(first, first+n, ispositive()); /*check are all of the elements positive?*/
#define any_pos( a , size ) any_of(first, first+n, ispositive()); /*check if there at least one positive element?*/
#define none_pos( a , size ) none_of(first, first+n, ispositive()); /*check are none of the elements positive?*/
#define Even(num) ((num&1)==1) ? false : true ;  //if returned false then number is odd, if returned true then number is even
#define FOREACH(i,t) for (__typeof(t.begin()) i=t.begin(); i!=t.end(); i++) //handels all data types,even pointers
#define For(i,a,b) for(size_t (i)=(a);(i)<=(b);++i) //Use:FOR(i,0,N) cin >> a[i]; //pointer other than int data type is not possible,and can itterate only max range of unsized int
#define For_0(i, n) For(i,0,b)
#define Rep(i,n) FOR(i,0,n)
#define For_(i,a,b) for(size_t (i)=(a); (i)>=(b);--i)
#define Rep_D(i,n) FOR_(i,n,0)
#define trav(a, x) for (auto& a : x) //Use:{ trav(x, v) cout << x << ' '; cout << endl; }
#define gchu(c) (c)=getchar_unlocked()
#define pchu(c) putchar_unlocked(c)
#define ABS(a) ((a) < 0 ? -(a) : (a))
#define MAX(i, j) (((i) > (j)) ? i : j)
#define MIN(i, j) (((i) < (j)) ? i : j) //use: MAX(a, b);
#define size(x) (int)(x).size()
#define B begin()
#define E end()
#define UB upper_bound
#define LB lower_bound
#define ALL(x) (x).begin(),(x).end()
#define PI 3.14159265 //  val = PI / 180.0; ret = cos( x*val ); //val = 180.0 / PI; ret = acos(x) * val;
#define fast cin.sync_with_stdio(0); cin.tie(0);\
	         cin.exceptions(cin.failbit)
#define ret return 0
#define gnum int num;\
    scanf("%d",&num)
#define pnum  printf("%d",num)
#define gstr char str[100];\
        scanf("%s",str)
#define pstr printf("%s",str)
